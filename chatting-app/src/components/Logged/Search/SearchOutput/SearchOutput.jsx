import React from "react";
import styled from "styled-components";

const Div = styled.div`

`;

const SearchOutput = ({ userList }) => {
    return (
        <Div>
        </Div>
    );
}

export default SearchOutput;